# 🚀 Первое развертывание OptiFi

Пошаговое руководство для первого развертывания проекта novaya-vetka на вашем VPS.

## 📋 Предварительная подготовка

### На локальной машине

1. **Убедитесь, что у вас есть:**
   - Git установлен
   - Доступ к репозиторию OptiFi
   - SSH ключ для подключения к VPS

2. **Подготовьте информацию:**
   - IP адрес VPS сервера
   - Пароль или SSH ключ для доступа
   - Email для SSL сертификата
   - Credentials из HackAPI (TEAM_CLIENT_SECRET)
   - Consent IDs для внешних банков

### На VPS сервере

1. **Убедитесь, что:**
   - Ubuntu 20.04 LTS или выше установлена
   - У вас есть доступ по SSH с правами sudo
   - Домен рефенансье.рф указывает на IP сервера

## 🎯 Вариант 1: Быстрое развертывание (рекомендуется)

### Шаг 1: Подключитесь к серверу

```bash
ssh root@your-vps-ip
# или
ssh user@your-vps-ip
sudo -i  # если нужны права администратора
```

### Шаг 2: Запустите скрипт развертывания

```bash
# Одна команда - всё установится автоматически!
curl -fsSL https://raw.githubusercontent.com/your-repo/OptiFi/novaya-vetka/deploy/deploy.sh | bash
```

**Что произойдет:**
- ✅ Обновится система
- ✅ Установится Docker и Docker Compose
- ✅ Установится Nginx и Certbot
- ✅ Клонируется репозиторий в `/opt/optifi`
- ✅ Создастся `.env` файл
- ✅ Запустятся Docker контейнеры
- ✅ Настроится Nginx
- ✅ Получится SSL сертификат
- ✅ Настроится firewall

### Шаг 3: Отредактируйте переменные окружения

```bash
# Отредактируйте .env с вашими credentials
nano /opt/optifi/.env

# Измените эти переменные:
# POSTGRES_PASSWORD=your-strong-password
# SECRET_KEY=your-long-random-key-min-32-chars
# TEAM_CLIENT_SECRET=your-credentials
# VBANK_PRODUCT_AGREEMENT_CONSENT_ID=your-consent-id
# ... и другие CONSENT_ID переменные
```

### Шаг 4: Перезагрузите приложение

```bash
cd /opt/optifi
docker-compose -f docker-compose.prod.yml restart
```

### Шаг 5: Проверьте, что всё работает

```bash
# Проверьте статус
cd /opt/optifi && bash deploy/manage.sh status

# Проверьте здоровье
cd /opt/optifi && bash deploy/manage.sh health

# Смотрите логи
cd /opt/optifi && bash deploy/manage.sh logs
```

### Шаг 6: Откройте приложение в браузере

```
https://рефенансье.рф
```

**Готово! 🎉**

---

## 🎯 Вариант 2: Пошаговое развертывание

Если вы хотите больше контроля над процессом, следуйте инструкциям в `DEPLOYMENT_QUICKSTART.md`.

---

## 🔍 Проверка после развертывания

### 1. Проверьте, что контейнеры запущены

```bash
cd /opt/optifi
docker-compose -f docker-compose.prod.yml ps

# Должны быть запущены:
# - optifi-db-prod (PostgreSQL)
# - optifi-api-prod (Bank API)
# - optifi-credit-analytics-prod (Credit Analytics)
```

### 2. Проверьте здоровье сервисов

```bash
cd /opt/optifi
bash deploy/manage.sh health

# Все три сервиса должны быть "Healthy"
```

### 3. Проверьте логи

```bash
cd /opt/optifi
bash deploy/manage.sh logs

# Не должно быть ошибок (ERROR)
```

### 4. Проверьте SSL сертификат

```bash
sudo certbot certificates

# Должен быть сертификат для рефенансье.рф
```

### 5. Проверьте Nginx

```bash
sudo nginx -t

# Должно быть: "test is successful"
```

### 6. Откройте приложение в браузере

```
https://рефенансье.рф
```

Должна загрузиться главная страница приложения.

---

## 🛠️ Основные команды управления

После развертывания используйте эти команды:

```bash
cd /opt/optifi

# Просмотр статуса
bash deploy/manage.sh status

# Просмотр логов (follow mode)
bash deploy/manage.sh logs

# Перезагрузка приложения
bash deploy/manage.sh restart

# Проверка здоровья
bash deploy/manage.sh health

# Резервная копия БД
bash deploy/manage.sh backup

# Справка
bash deploy/manage.sh help
```

---

## ⚠️ Частые ошибки и решения

### Ошибка: "Connection refused"

```bash
# Контейнеры не запущены или не готовы
cd /opt/optifi
docker-compose -f docker-compose.prod.yml logs

# Подождите 30 секунд и попробуйте снова
sleep 30
bash deploy/manage.sh health
```

### Ошибка: "SSL certificate not found"

```bash
# Сертификат еще не получен
# Проверьте, что DNS правильно указывает на сервер
nslookup рефенансье.рф

# Если DNS правильный, получите сертификат вручную
sudo certbot --nginx -d рефенансье.рф
```

### Ошибка: "Port already in use"

```bash
# Порт занят другим приложением
sudo netstat -tlnp | grep -E ':(80|443|8000|8100|5432)'

# Остановите конфликтующее приложение или измените порт
```

### Ошибка: "Database connection failed"

```bash
# БД не инициализирована
cd /opt/optifi
docker-compose -f docker-compose.prod.yml logs db

# Удалите volume и пересоздайте
docker-compose -f docker-compose.prod.yml down -v
docker-compose -f docker-compose.prod.yml up -d
```

---

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
cd /opt/optifi
bash deploy/manage.sh logs
```

### Просмотр логов конкретного сервиса

```bash
cd /opt/optifi

# Credit Analytics
bash deploy/manage.sh logs-analytics

# Bank API
bash deploy/manage.sh logs-api

# Database
bash deploy/manage.sh logs-db
```

### Проверка использования ресурсов

```bash
docker stats
```

### Nginx логи

```bash
# Access логи
sudo tail -f /var/log/nginx/optifi_access.log

# Error логи
sudo tail -f /var/log/nginx/optifi_error.log
```

---

## 🔄 Обновление приложения

Если вы хотите обновить код:

```bash
cd /opt/optifi

# Получите последние изменения
git pull origin novaya-vetka

# Пересоберите образы
docker-compose -f docker-compose.prod.yml build

# Перезагрузите контейнеры
docker-compose -f docker-compose.prod.yml up -d
```

Или используйте скрипт:

```bash
cd /opt/optifi
bash deploy/manage.sh update
```

---

## 💾 Резервные копии

### Создание резервной копии

```bash
cd /opt/optifi
bash deploy/manage.sh backup

# Создаст файл: backup_20250110_120000.sql
```

### Восстановление из резервной копии

```bash
cd /opt/optifi
bash deploy/manage.sh restore backup_20250110_120000.sql
```

---

## 🔐 Безопасность

### Измените пароли и ключи

```bash
nano /opt/optifi/.env

# Измените:
# POSTGRES_PASSWORD - сильный пароль (минимум 16 символов)
# SECRET_KEY - длинный случайный ключ (минимум 32 символа)
# TEAM_CLIENT_SECRET - ваши credentials

# Перезагрузите приложение
cd /opt/optifi
docker-compose -f docker-compose.prod.yml restart
```

### Обновляйте систему

```bash
sudo apt-get update
sudo apt-get upgrade -y
```

### Проверяйте логи безопасности

```bash
sudo tail -f /var/log/auth.log
```

---

## 📞 Помощь

Если у вас возникли проблемы:

1. **Проверьте логи:**
   ```bash
   cd /opt/optifi && bash deploy/manage.sh logs
   ```

2. **Проверьте здоровье:**
   ```bash
   cd /opt/optifi && bash deploy/manage.sh health
   ```

3. **Смотрите документацию:**
   - `DEPLOYMENT_QUICKSTART.md` - для типичных проблем
   - `DEPLOYMENT.md` - для подробного анализа
   - `deploy/README.md` - для описания файлов

4. **Проверьте DNS:**
   ```bash
   nslookup рефенансье.рф
   ```

---

## ✅ Чек-лист успешного развертывания

- [ ] Скрипт `deploy.sh` выполнился без ошибок
- [ ] Все три контейнера запущены (`bash deploy/manage.sh status`)
- [ ] Все сервисы здоровы (`bash deploy/manage.sh health`)
- [ ] SSL сертификат установлен (`sudo certbot certificates`)
- [ ] Приложение доступно по https://рефенансье.рф
- [ ] Логи не содержат ошибок (`bash deploy/manage.sh logs`)
- [ ] Создана резервная копия БД (`bash deploy/manage.sh backup`)

---

## 🎉 Поздравляем!

Ваш OptiFi успешно развернут на production! 🚀

**Приложение доступно по адресу:**
```
https://рефенансье.рф
```

**Для управления используйте:**
```bash
cd /opt/optifi
bash deploy/manage.sh [command]
```

---

**Дата:** 10 января 2025  
**Версия:** 1.0  
**Ветка:** novaya-vetka
