# 🚀 OptiFi Deployment

Скрипты и конфигурации для развертывания OptiFi на Ubuntu VPS.

## 📁 Содержимое папки

- **`deploy.sh`** - Основной скрипт автоматического развертывания на VPS
- **`test-local.sh`** - Скрипт для локального тестирования Docker конфигурации
- **`manage.sh`** - Скрипт управления приложением (логи, перезагрузка, резервные копии)
- **`nginx.conf`** - Конфигурация Nginx для reverse proxy и SSL
- **`.env.production`** - Шаблон переменных окружения для production
- **`DEPLOYMENT.md`** - Подробное руководство по развертыванию

## ⚡ Быстрый старт

### На локальной машине

```bash
# Тестируем Docker конфигурацию локально
bash deploy/test-local.sh
```

### На VPS сервере

```bash
# Подключаемся к серверу
ssh root@your-vps-ip

# Запускаем скрипт развертывания
curl -fsSL https://raw.githubusercontent.com/your-repo/OptiFi/novaya-vetka/deploy/deploy.sh | bash
```

## 📖 Подробное руководство

Для полного пошагового руководства смотрите **`DEPLOYMENT.md`**.

## 🛠️ Управление приложением

После развертывания используйте скрипт `manage.sh` для управления:

```bash
# На сервере
cd /opt/optifi

# Просмотр статуса
bash deploy/manage.sh status

# Просмотр логов
bash deploy/manage.sh logs

# Перезагрузка приложения
bash deploy/manage.sh restart

# Резервная копия БД
bash deploy/manage.sh backup

# Справка
bash deploy/manage.sh help
```

## 🔧 Конфигурация

### Переменные окружения

Все переменные окружения находятся в файле `.env` в корне приложения:

```bash
# На сервере
nano /opt/optifi/.env

# Важные переменные:
POSTGRES_PASSWORD=your-strong-password
SECRET_KEY=your-long-secret-key
TEAM_CLIENT_SECRET=your-credentials
```

### Nginx конфигурация

Nginx конфигурация автоматически копируется на сервер:

```bash
# На сервере
sudo nano /etc/nginx/sites-available/рефенансье.рф
sudo nginx -t
sudo systemctl restart nginx
```

## 📊 Мониторинг

### Просмотр логов

```bash
# Все логи
bash deploy/manage.sh logs

# Логи конкретного сервиса
bash deploy/manage.sh logs-analytics
bash deploy/manage.sh logs-api
bash deploy/manage.sh logs-db
```

### Проверка здоровья

```bash
bash deploy/manage.sh health
```

### Использование ресурсов

```bash
# На сервере
docker stats
```

## 🔄 Обновление приложения

```bash
# На сервере
cd /opt/optifi
bash deploy/manage.sh update
```

## 💾 Резервные копии

### Создание резервной копии

```bash
cd /opt/optifi
bash deploy/manage.sh backup
```

### Восстановление из резервной копии

```bash
cd /opt/optifi
bash deploy/manage.sh restore backup_20250110_120000.sql
```

## 🐛 Решение проблем

Если возникли проблемы, смотрите раздел "Решение проблем" в **`DEPLOYMENT.md`**.

## 📞 Поддержка

Для вопросов и проблем:

1. Проверьте логи: `bash deploy/manage.sh logs`
2. Проверьте здоровье: `bash deploy/manage.sh health`
3. Смотрите `DEPLOYMENT.md` для решения типичных проблем

## 🎯 Требования

- Ubuntu 20.04 LTS или выше
- Docker и Docker Compose
- Nginx
- Certbot для SSL
- Минимум 2GB RAM на сервере

## 📝 Примечания

- Все скрипты требуют прав sudo на сервере
- SSL сертификат автоматически обновляется каждые 90 дней
- Резервные копии БД сохраняются в текущей директории
- Логи приложения доступны через Docker

## 🔐 Безопасность

- Все пароли должны быть сильными (минимум 16 символов)
- SECRET_KEY должен быть длинным случайным ключом (минимум 32 символа)
- Регулярно обновляйте систему и Docker образы
- Используйте firewall для ограничения доступа

## ✅ Чек-лист перед развертыванием

- [ ] DNS рефенансье.рф указывает на IP сервера
- [ ] SSH доступ к серверу работает
- [ ] На сервере установлены Docker и Docker Compose
- [ ] Подготовлены все credentials (TEAM_CLIENT_SECRET, CONSENT_ID и т.д.)
- [ ] Создан сильный пароль для БД
- [ ] Создан длинный SECRET_KEY
- [ ] Email для SSL сертификата готов

## 🚀 После развертывания

1. Проверьте, что приложение доступно по https://рефенансье.рф
2. Проверьте логи на ошибки: `bash deploy/manage.sh logs`
3. Проверьте здоровье сервисов: `bash deploy/manage.sh health`
4. Настройте мониторинг и алерты
5. Создайте первую резервную копию: `bash deploy/manage.sh backup`

---

**Поздравляем с успешным развертыванием OptiFi! 🎉**
