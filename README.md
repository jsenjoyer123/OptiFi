# 📊 Credit Analytics Microservice

> Мультибанковский сервис кредитной аналитики и рефинансирования от команды «Тралалело» (капитан — Александр) для HackAPI 2025.

## ⚡️ Кратко

* __Назначение__ — агрегировать кредитные договоры клиента из собственного банка и внешних OpenBanking-провайдеров, рассчитывать персональные предложения по рефинансированию и оформлять заявки.
* __Стек__ — Node.js 20, Express, Axios, `helmet`, `cors`, `morgan`, Vue 3 (iframe-виджет во `frontend/credit-analytics/`).
* __Развёртывание__ — отдельный сервис на порту `8100`, проксирующий запросы в основной Bank-in-a-Box API и песочницу внешних банков.
* __Компоненты__ — сервер (`microservices/credit-analytics-backend/src/server.js`), конфигурация (`src/config/env.js`), бизнес-логика (`src/v1/routes/*.js`), интеграции (`src/lib/`).

## 🧠 Ключевые возможности

* __Агрегация договоров__ — `GET /api/loans/active` подтягивает внутренние `product-agreements`, балансы счетов и внешние кредиты (`src/v1/routes/loans.js`).
* __Подбор офферов__ — `GET /api/refinance/suggestions` считает ежемесячный платёж, экономию и рекомендуемые продукты (`src/v1/routes/recommendations.js`).
* __Подача заявок__ — `POST /api/refinance/applications` создаёт договор рефинансирования в Bank-in-a-Box с учётом лимитов продукта.
* __Здоровая конкуренция__ — мониторинг доступности внешних банков (`/api/refinance/banks/health`, `/api/refinance/status`) и поддержка white-label iframe.
* __Mock-режимы__ — быстрое демо через `USE_MOCK_DATA=true` и подстановку тестовых продуктов (`src/lib/mockData.js`).

## 🏗️ Архитектура взаимодействия

```mermaid
flowchart LR
    ParentApp[Основное приложение банка]
    Widget[Credit Analytics Widget (iframe)]
    Backend[Credit Analytics Backend (Express)]
    BankAPI[Bank-in-a-Box API]
    ExternalBanks[Внешние банки (OpenBanking API)]

    ParentApp -->|postMessage токен| Widget
    Widget -->|REST /api/...| Backend
    Backend -->|Proxy| BankAPI
    Backend -->|OAuth2 sandbox токены| ExternalBanks
    ExternalBanks -->|Счета, договоры, остатки| Backend
    BankAPI -->|Договоры, балансы| Backend
    Backend -->|Агрегированные данные| Widget
    Widget -->|UI события| ParentApp
```

## ⚙️ Компоненты каталога

* __Сервер Express__ — `src/server.js` включает защитные хедеры (`helmet`), CORS, JSON body parsing, статику Vue и health-check.
* __Конфигурация__ — `src/config/env.js` загружает `.env`, проверяет обязательные ключи, собирает список внешних банков и тайм-ауты.
* __Маршруты v1__ —
  * `src/v1/routes/loans.js` — агрегация кредитов и балансов.
  * `src/v1/routes/recommendations.js` — подбор офферов, мониторинг банков, создание заявок.
* __Интеграции__ — `src/lib/` (клиент Bank API, внешний сервис кредитов, mock-данные).
* __Фронтенд-виджет__ — `microservices/credit-analytics-backend/frontend/` (Vue 3 + Vite), поставляется как статика `dist/`.

## 🔌 API эндпоинты

| Метод | Путь | Назначение | Файл |
|-------|------|------------|------|
| GET | `/health` | Проверка статуса сервиса | `src/server.js` |
| GET | `/api/loans/active` | Объединённый список внутренних и внешних договоров | `src/v1/routes/loans.js` |
| GET | `/api/refinance/suggestions` | Расчёт персональных предложений | `src/v1/routes/recommendations.js` |
| POST | `/api/refinance/applications` | Создание договора рефинансирования | `src/v1/routes/recommendations.js` |
| GET | `/api/refinance/banks/health` | Быстрый health-check внешних банков | `src/v1/routes/recommendations.js` |
| GET | `/api/refinance/status` | Объединённый статус внешних банков (с тайм-аутами) | `src/v1/routes/recommendations.js` |

Все защищённые маршруты требуют заголовка `Authorization: Bearer <token>` из основного приложения.

## 🔐 Переменные окружения (`.env`)

| Переменная | Назначение | Значение по умолчанию |
|------------|------------|-----------------------|
| `PORT` | Порт сервиса | `8100` |
| `BANK_API_BASE_URL` | Базовый URL Bank-in-a-Box API | `http://localhost:8080` |
| `BANK_API_TIMEOUT_MS` | Тайм-аут запросов к Bank API | `10000` |
| `EXTERNAL_BANK_TIMEOUT_MS` | Тайм-аут запросов к внешним банкам | `10000` |
| `TEAM_CLIENT_ID`, `TEAM_CLIENT_SECRET` | креденшлы команды «Тралалело» | из песочницы HackAPI |
| `EXTERNAL_CLIENT_ID` | Идентификатор клиента для внешних запросов | примеры в `.env.example` |
| `VBANK_API_BASE`, `ABANK_API_BASE`, `SBANK_API_BASE` | URL песочниц внешних банков | см. `.env.example` |
| `VBANK_PRODUCT_AGREEMENT_CONSENT_ID`, `ABANK_PRODUCT_AGREEMENT_CONSENT_ID`, `SBANK_PRODUCT_AGREEMENT_CONSENT_ID` | Consent ID для договоров | см. `.env.example` |
| `VBANK_ACCOUNT_CONSENT_ID`, `ABANK_ACCOUNT_CONSENT_ID`, `SBANK_ACCOUNT_CONSENT_ID` | Consent ID для счетов | опционально |
| `USE_MOCK_DATA` | Включить mock-данные (`true/false`) | `false` |
| `USE_MOCK_EXTERNAL_BANKS` | Использовать моковые внешние банки | `false` |
| `FORCE_REAL_REFINANCE` | Форсировать реальные заявки, даже в mock-режиме | `false` |
| `LOCAL_PRODUCT_CATALOG_BASE_URL` | Локальный источник продуктового каталога | `http://localhost:8080` |
| `ADDITIONAL_PRODUCT_CATALOG_URL` | Дополнительный источник продуктов | `null` |

Полная заготовка доступна в `microservices/credit-analytics-backend/.env.example`.

## 🚀 Запуск и разработка

### Быстрый старт (npm)

```bash
cd microservices/credit-analytics-backend
cp .env.example .env
npm install
npm run dev
# сервис доступен на http://localhost:8100
```

### Продакшн-режим

```bash
cd microservices/credit-analytics-backend
npm install --production
npm run build   # сборка Vue-виджета, если требуется
npm start       # Express слушает PORT (по умолчанию 8100)
```

Для сборки фронтенда используйте скрипты в `frontend/` (Vite). Полученный `dist/` автоматически раздаётся `express.static` в `src/server.js`.

## 🔗 Интеграция с основным приложением

* Встраивайте виджет как iframe из `http://<host>:8100/` в клиентский портал. Токен авторизации передавайте через `window.postMessage`.
* Все защищённые запросы микросервис проксирует в Bank-in-a-Box с заголовком `Authorization`.
* Дополнительные банковские данные подгружаются через OpenBanking API песочниц по consent ID (см. `src/lib/externalLoansService.js`).

## 📈 Мониторинг и логирование

* `morgan('dev')` логирует HTTP-запросы.
* Централизованный перехват ошибок (`app.use` в `src/server.js`) возвращает JSON со статусом и стеком в DEV-режиме.
* Проверка внешних банков доступна через `/api/refinance/banks/health` (легковесный fetch) и `/api/refinance/status` (axios с тайм-аутами).

## 🛡️ Безопасность и соответствие

* Авторизация — Bearer-токены основного Bank-in-a-Box.
* Нет хранения чувствительных данных на стеке сервиса, все токены читаются из `.env` (см. требования в `docs/multibank-requirements.md`).
* Используются `helmet`, строгие CORS и ограниченный размер тела (1 МБ).

## 🗺️ Дорожная карта

* __Спринт 1__ — интеграция LLM для персональных пояснений выгоды рефинансирования.
* __Спринт 2__ — SDK для iOS/Android, упрощающие встраивание iframe-виджета в мобильные приложения.

## 📄 Лицензия

Проект распространяется по лицензии MIT. См. файл `LICENSE`.

